spring:
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false

management:
  security:
    enabled: false

eureka:
  client:
    enabled: false